function condition(num){
    return num > 0 ? "positive" : num < 0 ? "Negative" : "Zero"


}

console.log (condition(0));

// example of nested ternary operator, after ? is if conidtion is positive, after : is if negative.